services:
  scrum-poker:
    container_name: scrum-poker
    image: ${IMAGE:-ghcr.io/tommypreel/scrum-poker:latest}
    ports:
      - "3100:80"
    restart: always
    environment:
      - PORT=3100
    networks:
      - web

networks:
  web:
    external: true
    name: web
